pre-commit:
  parallel: true
  jobs:
    - name: format
      run: pnpm dlx oxfmt --write {staged_files}
      glob: "*"
      stage_fixed: true

    - name: lint
      run: pnpm dlx oxlint --fix {staged_files}
      glob: "*"
      stage_fixed: true
